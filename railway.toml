[build]
builder = "NIXPACKS"
buildCommand = "composer install --no-dev --optimize-autoloader && npm ci && npm run build && php artisan config:cache"

[deploy]
startCommand = "php artisan migrate --force && php -S 0.0.0.0:$PORT -t public"
